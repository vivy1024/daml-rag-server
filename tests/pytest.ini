[pytest]
addopts = --import-mode=importlib --ignore=tests/e2e --ignore=tests/legacy_task08_enhanced_logging

markers =
    slow: long-running tests (opt-in)
    e2e: end-to-end tests requiring live LLM (opt-in)
