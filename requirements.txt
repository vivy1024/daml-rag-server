# MCP Protocol
mcp>=0.9.0

# 向量化和嵌入
sentence-transformers>=2.2.0

# 存储
redis>=5.0.0
qdrant-client>=1.16.0,<1.17.0  # 向量数据库（锁定兼容服务端v1.15.1，minor差≤1）
aiomysql>=0.2.0  # MySQL异步连接池
neo4j>=5.14.0  # Neo4j异步驱动

# HTTP API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
aiohttp>=3.8.0
sse-starlette>=1.6.5  # SSE流式输出支持

# 配置管理
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 机器学习
numpy>=1.24.0  # Thompson Sampling所需

# 工具
PyYAML>=6.0.0
httpx>=0.24.0
psutil>=5.9.0  # 内存监控（VCP验证特性）
prometheus_client>=0.19.0  # Prometheus指标导出

# JWT认证
PyJWT>=2.8.0  # Internal JWT验证（权限系统重构）

# LangGraph Agent执行引擎
langgraph>=0.2.0
langchain-core>=0.3.0

# GraphRAG检索（neo4j官方包，替代自研Layer1+Layer2）
neo4j-graphrag[qdrant]>=1.13.0

# BM25混合检索（Phase 4A）
rank_bm25>=0.2.2
jieba>=0.42.1

# WebSearch 兜底（完全免费，无需 API Key）
duckduckgo-search>=6.0.0  # 或 ddgs>=7.0.0（新包名）
