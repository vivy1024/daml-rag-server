# 处理器与验证器

**版本**: v1.0.0
**创建日期**: 2026-02-28
**状态**: 已完成

---

## 概述

本文档详细说明DAML-RAG框架中的处理器（Processors）和验证器（Validators）组件，负责结果处理和参数/输出验证。

**代码路径**: `daml-rag-server/src/framework/`

---

## 1. 基础结果处理器

**文件路径**: `processors/base_processor.py`

### 核心功能

提供框架层通用结果处理器的默认实现，支持多种处理策略。

### 处理器接口

```python
class IResultProcessor(ABC):
    """结果处理器接口"""

    def __init__(self, domain: str):
        self.domain = domain
        self.is_initialized = False
        self.templates = {}
        self.formatters = {}

    async def initialize(self) -> bool:
        """初始化处理器"""
        pass

    async def process(
        self,
        raw_results: List[Any],
        user_profile: Dict[str, Any],
        options: Optional[ProcessingOptions] = None,
        **kwargs
    ) -> ProcessingResult:
        """处理结果"""
        pass
```

### 处理策略

```python
class ProcessingStrategy(str, Enum):
    """处理策略枚举"""
    SUMMARY = "summary"           # 摘要策略
    DETAILED = "detailed"         # 详细策略
    RECOMMENDATION = "recommendation"  # 推荐策略
```

### 处理选项

```python
class ProcessingOptions:
    """处理选项"""
    strategy: ProcessingStrategy = ProcessingStrategy.SUMMARY
    max_length: int = 500         # 最大长度
    include_sources: bool = True  # 包含信息源
    confidence_threshold: float = 0.5  # 置信度阈值
```

### 处理结果

```python
class ProcessingResult:
    """处理结果"""
    success: bool
    processed_text: str = ""
    raw_results: List[Any] = field(default_factory=list)
    reasoning: Dict[str, Any] = field(default_factory=dict)
    sources: List[Dict[str, Any]] = field(default_factory=list)
    confidence_score: float = 0.0
    processing_time_ms: float = 0.0
    metadata: Dict[str, Any] = field(default_factory=dict)
    error: Optional[str] = None
```

### 实现类

#### BaseResultProcessor

基础结果处理器实现，提供通用的结果处理功能：

```python
class BaseResultProcessor(IResultProcessor):
    """
    基础结果处理器实现

    提供通用的结果处理功能，子类可继承并覆盖特定方法
    """

    async def validate_results(self, raw_results: List[Any]) -> bool:
        """验证结果是否有效"""
        # 默认验证：确保有结果且不为空
        return len(raw_results) > 0

    async def extract_sources(
        self,
        raw_results: List[Any]
    ) -> List[Dict[str, Any]]:
        """提取信息源"""
        sources = []
        for result in raw_results:
            if isinstance(result, dict):
                source = result.get("source")
                if source:
                    sources.append(source)
        return sources

    async def calculate_confidence(
        self,
        raw_results: List[Any],
        user_profile: Dict[str, Any]
    ) -> float:
        """计算置信度"""
        # 默认计算：基于结果数量和一致性
        if not raw_results:
            return 0.0

        base_score = min(0.8, len(raw_results) * 0.1)

        # 调整分数：基于用户档案匹配
        if user_profile:
            matches = sum(1 for result in raw_results if self._matches_user_profile(result, user_profile))
            match_ratio = matches / len(raw_results)
            base_score += match_ratio * 0.2

        return min(1.0, base_score)
```

#### BaseSummarizationProcessor

专门提供摘要功能的处理器：

```python
class BaseSummarizationProcessor(BaseResultProcessor, ISummarizationProcessor):
    """基础摘要处理器实现"""

    async def summarize(
        self,
        raw_results: List[Any],
        max_length: int = 500,
        **kwargs
    ) -> ProcessingResult:
        """生成摘要"""

    async def extract_key_points(
        self,
        raw_results: List[Any],
        max_points: int = 5
    ) -> List[str]:
        """提取关键点"""
```

#### BaseRecommendationProcessor

专门提供推荐功能的处理器：

```python
class BaseRecommendationProcessor(BaseResultProcessor, IRecommendationProcessor):
    """基础推荐处理器实现"""

    async def generate_recommendations(
        self,
        raw_results: List[Any],
        user_profile: Dict[str, Any],
        num_recommendations: int = 5,
        **kwargs
    ) -> ProcessingResult:
        """生成推荐"""

    async def rank_results(
        self,
        raw_results: List[Any],
        user_profile: Dict[str, Any],
        **kwargs
    ) -> List[Dict[str, Any]]:
        """排序结果"""
```

---

## 2. 参数验证器

**文件路径**: `orchestration/parameter_validator.py`

### 核心功能

1. 验证参数是否符合Schema
2. 检查必需字段
3. 检查类型匹配（List、Dict、String等）
4. 生成详细错误信息（包含缺失参数名）
5. 优先使用Schema注册表进行验证
6. 支持枚举类型验证

### Requirements

- 2.1: 参数验证功能
- 2.3: Schema注册表优先验证
- 2.4: 枚举类型验证
- 2.5: 详细验证日志记录

### 验证结果

```python
class ValidationResult:
    """验证结果"""

    def __init__(self, is_valid: bool, errors: Optional[List[str]] = None):
        self.is_valid = is_valid
        self.errors = errors or []

    def __bool__(self):
        return self.is_valid

    def get_error_message(self) -> str:
        """获取错误信息字符串"""
        if not self.errors:
            return ""
        return "\n".join(self.errors)
```

### 验证流程

```python
def validate_params(
    self,
    params: Dict[str, Any],
    tool_name: str,
    tool_schema: Optional[Type[BaseModel]] = None,
    param_schema: Optional[Dict[str, str]] = None
) -> ValidationResult:
    """
    验证参数是否符合Schema

    优先级：
    1. Pydantic Schema（如果提供）
    2. Schema注册表（如果可用）
    3. param_schema字典
    """
    # 1. 优先使用Pydantic Schema
    if tool_schema:
        try:
            tool_schema(**params)
            return ValidationResult(is_valid=True)
        except ValidationError as e:
            # 处理验证错误
            ...

    # 2. 使用Schema注册表验证
    if self.schema_registry and self.schema_registry.has_schema(tool_name):
        return self._validate_with_registry_schema(params, tool_name)

    # 3. 使用param_schema字典验证
    if param_schema:
        # 检查必需字段和类型
        ...
```

### 验证类型

```python
def check_type_match(self, value: Any, expected_type: str) -> bool:
    """检查类型匹配"""
    type_checks = {
        "str": lambda v: isinstance(v, str),
        "string": lambda v: isinstance(v, str),
        "int": lambda v: isinstance(v, int) and not isinstance(v, bool),
        "integer": lambda v: isinstance(v, int) and not isinstance(v, bool),
        "float": lambda v: isinstance(v, (int, float)) and not isinstance(v, bool),
        "bool": lambda v: isinstance(v, bool),
        "boolean": lambda v: isinstance(v, bool),
        "list": lambda v: isinstance(v, list),
        "array": lambda v: isinstance(v, list),
        "dict": lambda v: isinstance(v, dict),
        "object": lambda v: isinstance(v, dict),
        "any": lambda v: True
    }
```

---

## 3. LLM输出验证器

**文件路径**: `validators/output_validator.py`

### 核心功能

在LLM分析引擎输出后进行安全验证：
1. 禁忌症交叉检查：推荐动作 vs 用户禁忌列表
2. 训练量范围检查：不超过用户能力的120%

### 验证失败处理

- 标记低置信度（confidence降至0.3）
- 附加安全警告到safety_reminders
- 不阻断响应（用户仍可看到建议，但有明确警告）

### 验证结果

```python
@dataclass
class ValidationResult:
    """验证结果"""
    is_valid: bool = True
    warnings: List[str] = field(default_factory=list)
    contraindication_violations: List[str] = field(default_factory=list)
    overload_violations: List[str] = field(default_factory=list)

    @property
    def has_violations(self) -> bool:
        return len(self.contraindication_violations) > 0 or len(self.overload_violations) > 0
```

### 验证逻辑

```python
class LLMOutputValidator:
    """LLM输出验证器"""

    # 训练量上限倍率（用户能力的120%）
    OVERLOAD_THRESHOLD = 1.2

    def validate(
        self,
        output_text: str,
        contraindications: Optional[List[Dict[str, Any]]] = None,
        user_capacity: Optional[Dict[str, Any]] = None,
    ) -> ValidationResult:
        """验证LLM输出"""
        result = ValidationResult()

        # 1. 禁忌症交叉检查
        if contraindications:
            self._check_contraindications(output_text, contraindications, result)

        # 2. 训练量范围检查
        if user_capacity:
            self._check_training_load(output_text, user_capacity, result)

        result.is_valid = not result.has_violations
        return result
```

### 禁忌症检查

```python
def _check_contraindications(
    self,
    output_text: str,
    contraindications: List[Dict[str, Any]],
    result: ValidationResult,
):
    """禁忌症交叉检查：扫描输出文本中是否推荐了禁忌动作"""
    text_lower = output_text.lower()

    for item in contraindications:
        name = item.get("name", item.get("exercise_name", ""))
        name_lower = name.lower()

        # 检查动作名是否出现在输出中（排除"禁忌"/"避免"/"不要"上下文）
        if name_lower in text_lower:
            # 检查是否在安全警告上下文中（这种情况是正确的）
            if self._is_in_warning_context(text_lower, name_lower):
                continue

            # 违规：推荐了禁忌动作
            violation_msg = f"输出中推荐了禁忌动作「{name}」"
            result.contraindication_violations.append(violation_msg)
```

### 训练量检查

```python
def _check_training_load(
    self,
    output_text: str,
    user_capacity: Dict[str, Any],
    result: ValidationResult,
):
    """训练量范围检查：确保不超过用户能力的120%"""
    # 检查重量
    max_weight = user_capacity.get("max_weight_kg")
    if max_weight:
        self._check_numeric_param(
            output_text, max_weight, "重量",
            [r"(\d+(?:\.\d+)?)\s*(?:kg|公斤|千克)"],
            result,
        )

    # 检查次数
    max_reps = user_capacity.get("max_reps")
    if max_reps:
        self._check_numeric_param(
            output_text, max_reps, "次数",
            [r"(\d+)\s*(?:次|reps|rep)"],
            result,
        )

    # 检查组数
    max_sets = user_capacity.get("max_sets")
    if max_sets:
        self._check_numeric_param(
            output_text, max_sets, "组数",
            [r"(\d+)\s*(?:组|sets|set)"],
            result,
        )
```

### 应用验证结果

```python
def apply_to_analysis(
    self,
    analysis_result: Any,
    validation: ValidationResult,
) -> Any:
    """将验证结果应用到AnalysisResult"""
    if not validation.has_violations:
        return analysis_result

    # 降低置信度
    analysis_result.confidence = min(analysis_result.confidence, 0.3)

    # 附加安全警告
    for violation in validation.contraindication_violations:
        warning = f"⚠️ 安全警告：{violation}"
        if warning not in analysis_result.safety_reminders:
            analysis_result.safety_reminders.append(warning)

    for violation in validation.overload_violations:
        warning = f"⚠️ 训练量警告：{violation}"
        if warning not in analysis_result.safety_reminders:
            analysis_result.safety_reminders.append(warning)

    # 在元数据中记录验证失败
    analysis_result.analysis_metadata["validation_failed"] = True

    return analysis_result
```

---

## 4. 参数验证中间件

**文件路径**: `orchestration/param_validation_middleware.py`

### 功能

FastAPI中间件，自动对请求参数进行验证。

---

## 相关链接

- **MCP工具注册**: [03-MCP工具实现/01-MCP工具注册表.md](./03-MCP工具实现/01-MCP工具注册表.md)
- **编排层架构**: [02-核心架构/03-编排层/01-DAG编排引擎.md](./02-核心架构/03-编排层/01-DAG编排引擎.md)

---

**维护者**: 薛小川
**最后更新**: 2026-02-28