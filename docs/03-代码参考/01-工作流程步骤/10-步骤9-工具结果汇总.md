# 步骤9 - 工具结果汇总

**版本**: v1.1.0
**创建日期**: 2026-02-21
**状态**: 持续更新

---

## 概述

步骤9将前面步骤的所有数据汇总成结构化 JSON，包括 MCP 工具调用结果、用户档案、会员信息、检索结果等。汇总后的数据作为步骤10（LLM深度分析）的输入。

### 在工作流中的位置

```
步骤7: DAG编排执行（dag_results）
步骤8: 三层检索（retrieval_results）
  ↓
步骤9: 工具结果汇总 ⭐ ← 当前
  ↓  输出 aggregated_data, _mcp_tools_called → 写入 WorkflowState
步骤10: LLM深度分析
```

---

## 源码位置

| 文件 | 说明 |
|------|------|
| `src/applications/fitness/workflow/nodes.py` (L1190-1269) | `node_aggregate_data` 主函数 |

---

## 函数签名

```python
async def node_aggregate_data(
    state: WorkflowState
) -> StateUpdate:
```

### 参数说明

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `state` | `WorkflowState` | ✅ | 当前工作流状态，包含 `request_id`、`dag_results`、`user_profile`、`membership_info`、`retrieval_results` |

### 返回值

```python
StateUpdate(updates={
    "aggregated_data": Dict[str, Any],  # 汇总后的所有数据
    "_mcp_tools_called": List[str],     # 成功调用的 MCP 工具列表
})
```

---

## 执行逻辑

### 1. 从 DAG 结果中提取 MCP 工具调用

```python
all_data = {}
mcp_tools_called = []

if dag_results:
    for task_name, task_result in dag_results.items():
        # 只记录真正的 MCP 工具
        if task_name in MCP_TASK_TYPES:
            if task_result and isinstance(task_result, dict):
                all_data[task_name] = task_result
                
                # 严格的成功判断
                has_error = "error" in task_result and task_result["error"] is not None
                is_success = task_result.get("success", False) is True
                is_fallback = task_result.get("fallback", False) or task_result.get("fallback_used", False)
                
                if is_success and not has_error and not is_fallback:
                    mcp_tools_called.append(task_name)
```

**MCP 工具类型**（`MCP_TASK_TYPES`）：
- `user_profile_query`
- `food_nutrition_query`
- `training_plan_generation`
- `exercise_recommendation`
- ...

**工作流程步骤**（`WORKFLOW_STEPS`）也会保存到 `all_data`，但不计入 `mcp_tools_called`。

### 2. 添加用户档案

```python
if user_profile:
    all_data["user_profile"] = user_profile
```

### 3. 添加会员信息

```python
if membership_info:
    all_data["user_membership"] = membership_info
```

### 4. 添加检索结果

```python
if retrieval_results and retrieval_results.get("results"):
    all_data["retrieval_results"] = {
        "results": retrieval_results.get("results", []),
        "count": retrieval_results.get("count", 0),
        "query_type": retrieval_results.get("query_type", "unknown")
    }
```

### 5. 统计和日志

```python
workflow_data_count = len([
    k for k in all_data.keys() 
    if k in WORKFLOW_STEPS or k in ['user_profile', 'user_membership', 'retrieval_results']
])

logger.info(f"✅ [{request_id}] 步骤9完成:")
logger.info(f"   - MCP工具调用: {len(mcp_tools_called)}个 ({', '.join(mcp_tools_called) if mcp_tools_called else '无'})")
logger.info(f"   - 工作流程数据: {workflow_data_count}项")
logger.info(f"   - 总数据项: {len(all_data)}个")
```

---

## aggregated_data 结构示例

```python
{
    # MCP 工具调用结果
    "user_profile_query": {
        "success": True,
        "data": {...},
        "error": None
    },
    "food_nutrition_query": {
        "success": True,
        "data": {...},
        "error": None
    },
    
    # 用户档案
    "user_profile": {
        "basic_info": {...},
        "fitness_config": {...},
        "health_profile": {...}
    },
    
    # 会员信息
    "user_membership": {
        "tier": "premium",
        "expires_at": "2024-12-31"
    },
    
    # 检索结果
    "retrieval_results": {
        "results": [...],
        "count": 10,
        "query_type": "hybrid_search"
    }
}
```

---

## 错误处理

本节点逻辑简单，无 try-except 包裹。所有输入均有默认值，不会抛出异常。

---

## 与后续步骤的数据流

### 输出到步骤10（LLM深度分析）

`state["aggregated_data"]` 作为 LLM 提示词的核心数据源，提供：
- MCP 工具返回的结构化数据
- 用户档案和会员信息
- 知识库检索结果

`state["_mcp_tools_called"]` 用于生成"数据来源"说明。

---

## 相关文档

- [09-步骤8-三层检索.md](./09-步骤8-三层检索.md)
- [11-步骤10-LLM深度分析.md](./11-步骤10-LLM深度分析.md)

---

**维护者**: 薛小川
**最后更新**: 2026-02-21
