# 步骤10 - LLM深度分析

**版本**: v1.1.0
**创建日期**: 2026-02-21
**状态**: 持续更新

---

## 概述

步骤10是工作流的**核心输出步骤**，负责调用 LLM 生成最终的专业回答。基于步骤9汇总的所有数据（MCP工具结果、用户档案、检索结果、Few-Shot示例），构建提示词并调用 LLM，支持多后端降级策略。

### 在工作流中的位置

```
步骤9: 工具结果汇总
  ↓  aggregated_data, _mcp_tools_called
步骤10: LLM深度分析 ⭐ ← 当前
  ↓  输出 final_response → 写入 WorkflowState
步骤11: 交互记录
```

---

## 源码位置

| 文件 | 说明 |
|------|------|
| `src/applications/fitness/workflow/nodes.py` (L1271-1445) | `node_llm_analysis` 主函数 |
| `src/applications/fitness/workflow/nodes.py` (L1447-1486) | `_build_fallback_response` 降级响应构建 |
| `src/framework/config/llm_response_config_manager.py` | `LLMResponseConfigManager` 配置管理器 |
| `src/framework/clients/llm_fallback_manager.py` | `LLMFallbackManager` 降级管理器 |

---

## 函数签名

```python
async def node_llm_analysis(
    state: WorkflowState,
    config_manager=None,
    fallback_manager=None,
    template_manager=None
) -> StateUpdate:
```

### 参数说明

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `state` | `WorkflowState` | ✅ | 当前工作流状态，包含所有前置步骤的输出 |
| `config_manager` | `LLMResponseConfigManager \| None` | ❌ | LLM响应配置管理器，未传入时自动创建 |
| `fallback_manager` | `LLMFallbackManager \| None` | ❌ | LLM降级管理器，未传入时自动创建 |
| `template_manager` | `DAGTemplateManager \| None` | ❌ | DAG模板管理器，用于获取 `response_hint` |

### 返回值

```python
StateUpdate(updates={
    "final_response": str,              # LLM生成的最终回答
    "_llm_backend_used": str,           # 使用的后端（anthropic/deepseek/template）
    "_llm_fallback_used": bool,         # 是否使用了降级策略
})
```

---

## 执行逻辑

### 1. 获取模板配置

```python
if config_manager is None:
    from ....framework.config.llm_response_config_manager import LLMResponseConfigManager
    config_manager = LLMResponseConfigManager()

llm_response_config = config_manager.get_config(selected_template_id)
# llm_response_config.max_tokens: 最大token数
# llm_response_config.temperature: 温度参数
```

### 2. 获取 response_hint

从 DAG 模板获取响应提示（如"请提供专业的训练计划"）：

```python
selected_template = template_manager.get_template(selected_template_id)
response_hint = selected_template.response_hint if selected_template else "请提供专业的分析和建议"
```

### 3. 准备用户档案字符串

```python
user_profile_for_prompt = '未提供'
if user_profile:
    # 序列化为 JSON，处理不可序列化对象
    serializable_profile = {}
    for key, value in user_profile.items():
        if key == 'membership' and hasattr(value, '__dict__'):
            serializable_profile['membership_tier'] = str(value.tier)
        elif isinstance(value, (str, int, float, bool, list, dict, type(None))):
            serializable_profile[key] = value
        else:
            serializable_profile[key] = str(value)
    user_profile_for_prompt = json.dumps(serializable_profile, ensure_ascii=False)
```

### 4. 准备 MCP 工具结果

特殊处理 `professional_program_designer`（训练计划生成）：

```python
if "professional_program_designer" in dag_results:
    program_result = dag_results["professional_program_designer"]
    
    # 使用 WeeklyPlanGenerator 转换为单周输出
    from ..services.weekly_plan_generator import WeeklyPlanGenerator
    weekly_generator = WeeklyPlanGenerator()
    
    weekly_plan = weekly_generator.generate_first_week(
        full_program=program_result,
        user_profile=user_profile,
        total_weeks=total_weeks
    )
    
    weekly_plan_output = weekly_generator.convert_to_output_format(weekly_plan)
    program_result["weekly_plan_output"] = weekly_plan_output
    program_result["output_mode"] = "weekly"
    
    mcp_tools_result_json = json.dumps(program_result, ensure_ascii=False, indent=2)
```

### 5. 构建提示词

```python
system_prompt = config_manager.build_prompt(
    template_id=selected_template_id,
    query=query_text,
    user_profile=user_profile_for_prompt,
    response_hint=response_hint,
    mcp_tools_count=len(mcp_tools_called),
    retrieval_count=len(retrieval_results.get('results', [])),
    mcp_tools_result=mcp_tools_result_json
)
```

### 6. 调用 LLM

```python
if fallback_manager is None:
    from ....framework.clients.llm_fallback_manager import LLMFallbackManager
    fallback_manager = LLMFallbackManager(
        primary_backend="anthropic",
        fallback_backends=["deepseek", "template"],
        max_retries=3,
        timeout=30,
        enable_health_check=True
    )

llm_request = LLMRequest(
    query=query_text,
    few_shot_examples=few_shot_dicts,
    tool_results=aggregated_data,
    system_prompt=system_prompt,
    max_tokens=llm_response_config.max_tokens,
    temperature=llm_response_config.temperature,
    stream=False,
    conversation_history=conversation_history if conversation_history else None,
)

llm_response = await fallback_manager.call_with_fallback(llm_request)
final_response = llm_response.content
```

### 降级链

```
Anthropic Claude（主后端）
  ↓ 失败
DeepSeek（降级后端）
  ↓ 失败
Template（模板响应，最终降级）
```

---

## 错误处理

| 场景 | 处理方式 | 返回值 |
|------|---------|--------|
| LLM 调用异常 | 调用 `_build_fallback_response()` | 基于检索结果的降级响应 + `error` |
| 用户档案序列化失败 | 使用 `str(user_profile)` | 继续执行 |
| 分周计划生成失败 | 使用完整计划 | 继续执行 |

### `_build_fallback_response` 降级响应

当 LLM 完全失败时，基于检索结果构建简单响应：

```python
def _build_fallback_response(
    query_text: str,
    user_profile: Optional[Dict[str, Any]],
    results_data: list,
    error: str
) -> str:
    # 返回格式：
    # "抱歉，系统暂时无法生成完整回答。以下是相关信息：\n\n[检索结果摘要]"
```

---

## 与后续步骤的数据流

### 输出到步骤11（交互记录）

`state["final_response"]` 作为 LLM 响应内容保存到数据库。

---

## 相关文档

- [10-步骤9-工具结果汇总.md](./10-步骤9-工具结果汇总.md)
- [12-步骤11-交互记录.md](./12-步骤11-交互记录.md)
- [11.1-步骤10-提示词构建与LLM参数配置.md](./11.1-步骤10-提示词构建与LLM参数配置.md)（详细说明提示词构建逻辑）

---

**维护者**: 薛小川
**最后更新**: 2026-02-21
