# DAML-RAG 环境变量配置指南

**创建日期**: 2025-12-22

---


**版本**: v1.1.0
**更新日期**: 2026-02-20
**状态**: ✅ 生产就绪

---

## 📋 概述

本文档定义了DAML-RAG框架的所有环境变量配置，用于替代硬编码数据，确保生产环境的安全性和灵活性。

---

## 🔧 核心配置

### 数据库连接

```bash
# Neo4j 图数据库
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
NEO4J_DATABASE=neo4j

# Qdrant 向量数据库
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=training_knowledge
QDRANT_API_KEY=

# MySQL 后端数据库
DB_HOST=fitness_mysql
DB_PORT=3306
DB_DATABASE=fitness_app
DB_USERNAME=fitness_user
DB_PASSWORD=fitness_password

# Redis 缓存
REDIS_HOST=fitness_redis
REDIS_PORT=6379
REDIS_PASSWORD=
```

### MCP 服务配置

```bash
# MCP 服务器注册表
MCP_REGISTRY_CONFIG_PATH=/app/config/mcp_registry.json

# HTTP MCP 服务地址
MCP_USER_PROFILE_URL=http://localhost:3001
MCP_COACH_URL=http://localhost:3002

# MCP 连接池配置
MCP_POOL_TIMEOUT=30
MCP_POOL_MAX_RETRIES=3
MCP_POOL_RETRY_DELAY=1.0
MCP_HEALTH_CHECK_INTERVAL=60
```

### AI 模型配置

```bash
# 教师模型
TEACHER_MODEL_PROVIDER=deepseek
TEACHER_MODEL_NAME=deepseek-chat
TEACHER_MODEL_API_KEY=your_deepseek_api_key
TEACHER_MODEL_BASE_URL=https://api.deepseek.com

# 学生模型
STUDENT_MODEL_PROVIDER=ollama
STUDENT_MODEL_NAME=qwen3:8b
STUDENT_MODEL_BASE_URL=http://ollama:11434

# 双模型阈值
MODEL_FEW_SHOT_THRESHOLD=3
MODEL_SIMILARITY_THRESHOLD=0.7
```

### 编码和嵌入模型

```bash
# BGE 嵌入模型
EMBEDDING_MODEL_NAME=BAAI/bge-m3
EMBEDDING_MODEL_DEVICE=cpu
EMBEDDING_MODEL_CACHE_DIR=/app/models/embeddings
```

---

## 🚀 系统性能配置

```bash
# 并发和缓存
MCPOrchestrator_CACHE_TTL=300
MCPOrchestrator_MAX_PARALLEL=5
VECTOR_SEARCH_BATCH_SIZE=100
NEO4J_CONNECTION_POOL_SIZE=10

# 日志配置
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_FILE=/app/logs/daml_rag.log
```

---

## 🔐 安全配置

```bash
# 生产环境标识
ENVIRONMENT=production
DEBUG=false
ALLOW_DEMO_DATA=false

# 安全限制
MAX_REQUEST_SIZE_MB=10
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
```

---

## 🏃‍♂️ 健身领域配置

```bash
# 训练知识集合
FITNESS_TRAINING_COLLECTION=training_knowledge
FITNESS_EXERCISES_COLLECTION=fitness_exercises_v2
FITNESS_NUTRITION_COLLECTION=food_nutrition_vector

# 专业配置
STANDARDS_VALIDATION_ENABLED=true
SAFETY_RULES_ENABLED=true
MEDICAL_SCREENING_ENABLED=true
```

---

## 🧪 测试环境配置

```bash
# 仅在测试环境使用
DEMO_MODE=false
ALLOW_TEST_USERS=false
TEST_USER_ID=1
DEMO_USER_ID=demo_user

# 测试数据配置
SAMPLE_DATA_PATH=/app/tests/fixtures/sample_profiles.json
```

---

## 📝 使用示例

### 1. Docker 环境变量文件 (.env)

```bash
# 生产环境配置
ENVIRONMENT=production
DEBUG=false
ALLOW_DEMO_DATA=false

# 数据库连接
NEO4J_URI=bolt://neo4j:7687
NEO4J_PASSWORD=${NEO4J_PASSWORD_SECRET}
QDRANT_HOST=qdrant
REDIS_HOST=fitness_redis

# AI 模型
TEACHER_MODEL_API_KEY=${DEEPSEEK_API_KEY}
STUDENT_MODEL_BASE_URL=http://ollama:11434
```

### 2. 代码中使用环境变量

```python
import os
from typing import Optional

class Config:
    """DAML-RAG 配置类"""

    # Neo4j 配置
    NEO4J_URI: str = os.getenv('NEO4J_URI', 'bolt://localhost:7687')
    NEO4J_USER: str = os.getenv('NEO4J_USER', 'neo4j')
    NEO4J_PASSWORD: str = os.getenv('NEO4J_PASSWORD', '')

    # MCP 配置
    MCP_POOL_TIMEOUT: int = int(os.getenv('MCP_POOL_TIMEOUT', '30'))
    MCP_POOL_MAX_RETRIES: int = int(os.getenv('MCP_POOL_MAX_RETRIES', '3'))

    # AI 模型配置
    TEACHER_MODEL_NAME: str = os.getenv('TEACHER_MODEL_NAME', 'deepseek-chat')
    STUDENT_MODEL_NAME: str = os.getenv('STUDENT_MODEL_NAME', 'qwen3:8b')

    # 安全配置
    ALLOW_DEMO_DATA: bool = os.getenv('ALLOW_DEMO_DATA', 'false').lower() == 'true'
    ENVIRONMENT: str = os.getenv('ENVIRONMENT', 'development')

    @classmethod
    def is_production(cls) -> bool:
        """判断是否为生产环境"""
        return cls.ENVIRONMENT == 'production'

    @classmethod
    def get_user_id(cls, request) -> Optional[int]:
        """从请求中获取用户ID（生产环境实现）"""
        if cls.is_production():
            # 从session或JWT token获取真实用户ID
            return request.session.get('user_id') or request.user.id
        else:
            # 测试环境可返回固定ID（仅用于开发）
            test_id = os.getenv('TEST_USER_ID')
            return int(test_id) if test_id else None
```

### 3. 替换硬编码示例

```python
# ❌ 之前的硬编码做法
user_id = 1  # 硬编码
profile = await client.get_user_profile(user_id=1)

# ✅ 现在的环境变量做法
config = Config()
user_id = config.get_user_id(request)  # 动态获取
if user_id:
    profile = await client.get_user_profile(user_id)
else:
    raise AuthenticationError("用户未认证")
```

---

## 🔍 配置验证

### 启动时检查清单

```bash
# 必需的环境变量检查
echo "检查Neo4j配置: $NEO4J_URI"
echo "检查Qdrant配置: $QDRANT_HOST"
echo "检查AI模型配置: $TEACHER_MODEL_API_KEY"
echo "检查安全配置: $ALLOW_DEMO_DATA"
```

### Python 配置验证

```python
def validate_config() -> List[str]:
    """验证配置完整性"""
    errors = []

    # 检查必需配置
    required_vars = ['NEO4J_URI', 'QDRANT_HOST', 'TEACHER_MODEL_API_KEY']
    for var in required_vars:
        if not os.getenv(var):
            errors.append(f"缺少必需环境变量: {var}")

    # 生产环境特殊检查
    if Config.is_production():
        if Config.ALLOW_DEMO_DATA:
            errors.append("生产环境不允许启用演示数据")
        if not Config.TEACHER_MODEL_API_KEY:
            errors.append("生产环境必须设置AI模型API密钥")

    return errors
```

---

## 🚨 安全注意事项

### 1. 敏感信息管理
- 所有API密钥、密码必须通过环境变量注入
- 不要在代码中硬编码任何敏感信息
- 使用Docker secrets或Kubernetes secrets管理敏感配置

### 2. 生产环境安全
```bash
# 禁用所有测试和演示功能
ALLOW_DEMO_DATA=false
DEBUG=false
DEMO_MODE=false
```

### 3. 最小权限原则
- 数据库用户只授予必要权限
- API密钥使用最小作用域
- 网络访问限制在必要范围内

---

## 📊 监控和日志

```bash
# 配置监控指标
CONFIG_RELOAD_TIME=2025-12-02T10:00:00Z
CONFIG_VALIDATION_STATUS=passed
ENVIRONMENT_TYPE=production
SECURITY_LEVEL=high
```

---

## 🔗 相关文档

- [Docker部署指南](./Docker部署.md)
- <!-- [系统架构](./系统架构.md) (文档不存在) -->
- <!-- [安全最佳实践](./安全最佳实践.md) (文档不存在) -->

---

**维护者**: 薛小川
**最后审查**: 2026-02-19
**下次更新**: 配置变更时

---

## 🔀 多模型集成配置（v9.77.0 新增）

> 支持 Qwen、SiliconFlow、GLM 等 OpenAI 兼容后端，通过环境变量按需启用。

### 通用 OpenAI 兼容后端

```bash
# ── Qwen（通义千问）──
QWEN_ENABLED=false                          # 是否启用
QWEN_API_KEY=                               # API 密钥
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-plus                        # 模型名

# ── SiliconFlow（硅基流动）──
SILICONFLOW_ENABLED=false
SILICONFLOW_API_KEY=
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_MODEL=Qwen/Qwen2.5-7B-Instruct

# ── GLM（智谱清言）──
GLM_ENABLED=false
GLM_API_KEY=
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
GLM_MODEL=glm-4-flash
```

### 模板级路由

```bash
# 格式: template_id:backend,template_id:backend
# 未配置的模板走默认降级链
TEMPLATE_MODEL_MAP=
# 示例: greeting:siliconflow,quick_consultation:siliconflow,training_plan:anthropic
```

### Agent 模式后端

```bash
# Agent 模式使用的 LLM 后端（可选，不设则走默认 APIPoolManager）
# 可选值: qwen / siliconflow / glm
AGENT_LLM_BACKEND=
```

### 内部评估 API

```bash
# 模型评估统计端点认证（与 INTERNAL_API_TOKEN 共用）
# GET /api/internal/model-evaluation/stats
# 访问方式: 127.0.0.1 本地访问 或 Bearer Token
INTERNAL_API_TOKEN=crewai-internal-secret-2025
```

---

## 🚀 Zeabur 部署 Checklist（Phase 7/8 新增）

> 下次部署前必须在 Zeabur Secrets 中配置以下变量

### P0 必须配置（部署前）

| 变量 | 说明 | 示例值 |
|------|------|--------|
| `ANTHROPIC_API_KEY` | Kiro RS 代理 API 密钥 | `sk-ant-...` |
| `ANTHROPIC_BASE_URL` | Kiro RS 代理地址 | `https://api.kirors.com/v1` |
| `ANTHROPIC_MODEL` | Anthropic 模型名 | `claude-haiku-4-5-20250315` |
| `ANTHROPIC_ENABLED` | 启用 Anthropic 后端 | `true` |
| `BYPASS_RATE_LIMIT_FOR_INTERNAL` | 内部调用免限流 | `true` |
| `CORS_EXTRA_ORIGINS` | CORS 额外域名 | `https://yuzhen.fit,https://www.yuzhen.fit` |
| `LLM_PRIMARY_BACKEND` | 主 LLM 后端 | `anthropic` |

### P1 建议配置

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `LLM_MAX_RETRIES` | LLM 最大重试次数 | `3` |
| `LLM_RETRY_DELAY` | LLM 重试延迟(秒) | `1.0` |
| `MAX_CONCURRENT` | 最大并发请求数 | `10` |
| `MAX_PROMPT_CHARS` | 最大提示词字符数 | `10000` |
| `WORKFLOW_TIMEOUT` | 工作流超时(秒) | `120` |
| `MONITORING_ENABLED` | 启用监控 | `false` |

### 已有变量确认

以下变量应已在 Zeabur 中配置，确认值正确即可：
- `INTERNAL_API_TOKEN`（≥32字符）
- `INTERNAL_JWT_SECRET`（与 PHP 后端一致）
- `ENABLE_AUTH=true`
- `DEEPSEEK_API_KEY`
- 数据库连接（MySQL/Neo4j/Qdrant/Redis）

---

<div align="center">
<strong>🔧 配置化 · 🔐 安全性 · 🚀 生产就绪 🔧</strong>
</div>